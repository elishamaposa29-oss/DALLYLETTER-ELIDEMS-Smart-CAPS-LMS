import { useEffect, useMemo, useRef, useState } from "react";
import { useLocation, useRoute } from "wouter";
import { getApiUrl } from "@workspace/api-client-react";
import { DashboardLayout } from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Textarea } from "@/components/ui/textarea";
import { Badge } from "@/components/ui/badge";
interface Question{id:number;prompt:string;type:"input"|"poll"|"drawbox";marksAllocated:string;position:number;config:Record<string,unknown>};interface Option{id:number;questionId:number;label:string;value:string;position:number};interface Exercise{id:number;title:string;instructions:string|null;totalMarks:string};
function DrawBox({value,onChange}:{value:Record<string,unknown>|undefined;onChange:(v:Record<string,unknown>)=>void}){const ref=useRef<HTMLCanvasElement>(null);const drawing=useRef(false);useEffect(()=>{const c=ref.current;if(!c)return;const ctx=c.getContext("2d");if(!ctx)return;ctx.clearRect(0,0,c.width,c.height);ctx.lineWidth=2;ctx.lineCap="round";const strokes=(value?.strokes??[]) as {x:number;y:number;px:number;py:number}[];for(const s of strokes){ctx.beginPath();ctx.moveTo(s.px,s.py);ctx.lineTo(s.x,s.y);ctx.stroke()}},[value]);const point=(e:React.PointerEvent)=>{const c=ref.current;if(!c)return;const r=c.getBoundingClientRect();return{x:e.clientX-r.left,y:e.clientY-r.top}};const down=(e:React.PointerEvent)=>{drawing.current=true;ref.current?.setPointerCapture(e.pointerId)};const move=(e:React.PointerEvent)=>{if(!drawing.current)return;const p=point(e);if(!p)return;const strokes=[...((value?.strokes??[]) as {x:number;y:number;px:number;py:number}[])];const prev=strokes.at(-1);const ctx=ref.current?.getContext("2d");if(ctx){ctx.lineWidth=2;ctx.lineCap="round";ctx.beginPath();ctx.moveTo(prev?.x??p.x,prev?.y??p.y);ctx.lineTo(p.x,p.y);ctx.stroke()}onChange({strokes:[...strokes,{x:p.x,y:p.y,px:prev?.x??p.x,py:prev?.y??p.y}]})};return <div><canvas ref={ref} width={900} height={320} className="w-full touch-none rounded-lg border bg-white" onPointerDown={down} onPointerMove={move} onPointerUp={()=>{drawing.current=false}}/><Button type="button" size="sm" variant="ghost" onClick={()=>{const c=ref.current;const ctx=c?.getContext("2d");if(c&&ctx){ctx.clearRect(0,0,c.width,c.height);onChange({strokes:[]})}}}>Clear drawing</Button></div>}
export default function StudentExercise(){const[,params]=useRoute("/student/exercises/:id");const[,navigate]=useLocation();const id=params?.id;const[data,setData]=useState<{exercise:Exercise;questions:Question[];options:Option[]}|null>(null);const[answers,setAnswers]=useState<Record<number,{textAnswer?:string;selectedValue?:string;drawData?:Record<string,unknown>}>>({});const[message,setMessage]=useState("");const[result,setResult]=useState<any>(null);const[busy,setBusy]=useState(false);useEffect(()=>{if(!id)return;const token=localStorage.getItem("dallyletter_token");const headers=token?{Authorization:`Bearer ${token}`}:{};Promise.all([fetch(getApiUrl(`/api/exercises/${id}`),{headers}).then(async r=>{if(!r.ok)throw new Error("Exercise unavailable");return r.json()}),fetch(getApiUrl(`/api/exercises/${id}/result`),{headers}).then(async r=>r.ok?r.json():null).catch(()=>null)]).then(([exerciseData,resultData])=>{setData(exerciseData);setResult(resultData);}).catch(e=>setMessage(e instanceof Error?e.message:"Exercise unavailable"));},[id]);const optionsByQuestion=useMemo(()=>{const m:Record<string,Option[]>={};for(const o of data?.options??[])(m[o.questionId]??=[]).push(o);return m},[data]);const update=(qid:number,patch:Record<string,unknown>)=>setAnswers(a=>({...a,[qid]:{...a[qid],...patch}}));const submit=async()=>{if(!data)return;setBusy(true);setMessage("");try{const token=localStorage.getItem("dallyletter_token");const r=await fetch(getApiUrl(`/api/exercises/${data.exercise.id}/submit`),{method:"POST",headers:{"Content-Type":"application/json",...(token?{Authorization:`Bearer ${token}`}:{})},body:JSON.stringify({answers:Object.entries(answers).map(([questionId,a])=>({questionId:Number(questionId),...a}))})});if(!r.ok)throw new Error((await r.json().catch(()=>null))?.error||"Submission failed");setResult(null);setMessage("Submitted. Your teacher can now mark it. You can return here later to preview the marked result.")}catch(e){setMessage(e instanceof Error?e.message:"Submission failed")}finally{setBusy(false)}};if(!data)return <DashboardLayout><div className="p-6">{message||"Loading exercise…"}</div></DashboardLayout>;const isMarked=result?.submission?.status==="marked";return <DashboardLayout><div className="mx-auto max-w-3xl space-y-5 p-4"><Card><CardHeader><CardTitle>{data.exercise.title}</CardTitle>{data.exercise.instructions&&<p className="text-sm text-muted-foreground">{data.exercise.instructions}</p>}<div className="flex flex-wrap items-center gap-2"><Badge variant="outline">{data.exercise.totalMarks} marks</Badge><Badge variant={isMarked?"default":"secondary"}>{isMarked?"Marked • Result returned":"Open • Awaiting submission/marking"}</Badge></div></CardHeader></Card>{data.questions.map((q,i)=><Card key={q.id}><CardHeader><CardTitle className="text-base">Q{i+1}. {q.prompt}</CardTitle><Badge variant="secondary" className="w-fit">{q.marksAllocated} marks · {q.type}</Badge></CardHeader><CardContent>{q.type==="input"&&<Textarea disabled={isMarked} value={answers[q.id]?.textAnswer??""} onChange={e=>update(q.id,{textAnswer:e.target.value})} placeholder={isMarked?"Marked answer — read-only":"Type your answer…"}/>}{q.type==="poll"&&<div className="space-y-2">{(optionsByQuestion[String(q.id)]??[]).map(o=><label key={o.id} className="flex cursor-pointer gap-3 rounded-lg border p-3"><input disabled={isMarked} type="radio" name={`q-${q.id}`} checked={answers[q.id]?.selectedValue===o.value} onChange={()=>update(q.id,{selectedValue:o.value})}/><span>{o.label}</span></label>)}</div>}{q.type==="drawbox"&&<DrawBox value={answers[q.id]?.drawData} onChange={drawData=>update(q.id,{drawData})}/>}</CardContent></Card>)}<div className="flex items-center justify-between gap-3"><span className="text-sm text-muted-foreground">{message}</span>{!isMarked&&<Button onClick={submit} disabled={busy}>{busy?"Submitting…":"Submit Exercise"}</Button>}</div>{result?.submission?.status==="marked"&&<Card><CardHeader><CardTitle>Returned result</CardTitle></CardHeader><CardContent><p className="text-lg font-semibold">{result.submission.totalScore} / {data.exercise.totalMarks} marks</p><p className="text-sm text-muted-foreground">{result.submission.percentage}%</p><div className="mt-3 space-y-2">{(result.answers??[]).map((a:any)=><div key={a.id} className="rounded-lg border p-3"><div className="font-medium">{a.awardedMarks} marks</div>{a.correctionNotes&&<p className="text-sm text-muted-foreground mt-1">{a.correctionNotes}</p>}</div>)}</div></CardContent></Card>}<Button variant="ghost" onClick={()=>navigate("/student/lessons")}>Back to lessons</Button></div></DashboardLayout>}
